[package]
name = "http-router"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
anyhow = "1.0.100"
bytes = "1.11.1"
http = "1.3.1"
http-body-util = "0.1.3"
spin-sdk = { path = "../..", features = ["router"] }

[workspace]
