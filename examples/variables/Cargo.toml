[package]
name = "spin-variables-example"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
# Useful crate to handle errors.
anyhow = "1"
# HTTP types
http = "1.3"
# The Spin SDK.
spin-sdk = { path = "../../crates/spin-sdk" }
